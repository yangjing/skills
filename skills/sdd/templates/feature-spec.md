# <功能名>

> **Status**: draft | active | deprecated · **Owner**: `<负责人>` · **LastUpdated**: `<YYYY-MM-DD>` · **Compliance**: L0 | L1 | L2
> **规范语言**：BCP 14（RFC 2119/8174）

<!--
本模板对应 SPECIFICATION §4.1「功能规格文档最小结构」，适用于 §4.1.1 判定的 ① 类文档
（承载产品功能范围的功能 / 系统规格）。

② 总纲 / 索引 / 词表 与 ③ 技术设计 / 架构裁决 MUST NOT 套用本模板全结构——
强套会制造第二份功能范围声明。各体裁的结构要求见 SPECIFICATION §4.1.1。

Compliance 取档按所辖 Contract Surface 与数据面的最高敏感度，判据见 §4.1。
写每一节前先过 §1.0 内容准入：「删掉它，只读代码的人会失去什么？」
-->

## 1. 目标

**Why**：<为什么需要它——要解决的问题与它成立的前提>

**InScope**：

- <本次交付覆盖的能力>

**OutOfScope**：

- <明确排除的能力，以及排除的理由或推迟条件>

## 2. 边界

| 项 | 值 |
| --- | --- |
| BoundedContext | `<上下文名>` |
| Producer（SoR） | `<该类事实的唯一写入方>` |
| Consumer | `<消费方清单>` |
| 依赖强度 | 运行时强依赖 / 事实输入 / 治理 / 可选增强 |

## 3. 契约

<!-- 跨边界面 MUST 显式写明（§1.0 豁免第一条：消费方不该被要求去读提供方实现） -->

| 类别 | 内容 | 真相源 |
| --- | --- | --- |
| API | `<service / method>` | `<契约目录中的文件>` |
| 事件 | `<事件名与载荷>` | `<file>` |
| Schema | `<表 / 索引 / 约束>` | `<file>` |
| 错误码 | `<可枚举错误码及触发条件>` | `<file>` |
| 权限码 | `<resource:action>` | `<file>` |

**字段映射**（概念 ↔ API ↔ 策略）：

| 概念 | API 字段 | 策略 / schema 字段 |
| --- | --- | --- |
| `<概念名>` | `<wire 字段>` | `<策略契约字段>` |

## 4. 场景（BDD）

<!--
§4.2：异常路径数量 MUST 至少为正常路径的 1.5 倍。
最少覆盖：成功、权限拒绝、参数校验失败、依赖降级/超时、回滚/补偿。
每个场景 SHOULD 指明 Verification Oracle。
-->

### 4.1 正常路径

**场景：<名称>**

- **Given** <前置状态>
- **When** <触发动作>
- **Then** <可观察结果>
- **Oracle**: `<判定机制>`

### 4.2 异常路径

**场景：权限拒绝**

- **Given** <调用方缺少 `<权限码>`>
- **When** <触发动作>
- **Then** <拒绝形态与错误码；MUST 说明是否泄漏资源存在性>
- **Oracle**: `<判定机制>`

**场景：参数校验失败**

- **Given** <非法输入>
- **When** <触发动作>
- **Then** <错误码 + 错误信息含字段名>
- **Oracle**: `<判定机制>`

**场景：依赖降级 / 超时**

- **Given** <下游不可用>
- **When** <触发动作>
- **Then** <fail-closed 还是降级？降级后的语义边界是什么>
- **Oracle**: `<判定机制>`

**场景：回滚 / 补偿**

- **Given** <部分成功状态>
- **When** <触发回滚>
- **Then** <最终一致性达成方式与可观测信号>
- **Oracle**: `<判定机制>`

## 5. 数据

| 项 | 值 |
| --- | --- |
| 数据所有权 | `<谁拥有写入权与语义演进责任>` |
| 复制 / 一致性 | `<如有：复制方式与一致性模型>` |
| MaxStaleness | `<如有：可接受的最大陈旧度>` |

## 6. 开发与测试资产

| 资产 | 落点 |
| --- | --- |
| 测试 fixtures | `<path>` |
| `schema.sql` | `<path>` |
| `seed.sql` | `<path>` |

## 7. 验证

<!-- 无机器判定者的验收 MUST 标记 Human Approval Gate 并写明 evidence 形态 -->

| Acceptance | Verification Oracle | Evidence |
| --- | --- | --- |
| `<验收条款>` | `<类型检查 / 契约测试 / 自动化测试 / 人工 approval>` | `<证据形态>` |

## 8. 安全与审计

| 项 | 口径 |
| --- | --- |
| 最小权限 | `<所需权限码与授权边界>` |
| 敏感数据 | `<字段分级、加密与脱敏要求>` |
| 审计点 | `<记录哪些动作、proof 形态>` |

## 9. 演进

| 项 | 口径 |
| --- | --- |
| 兼容窗口 | `<窗口长度与判据>` |
| 灰度 / 回滚 | `<方案>` |
| 废弃策略 | `<通知周期与下线条件>` |
